export enum MuiColorEnum {
    //"primary" | "secondary" | "info" | "success" | "warning" | "inherit" | "default"
    PRIMARY = "primary",
    SECONDARY = "secondary",
    INFO = "info",
    SUCCES = "success",
    WARNING = "warning",
    INHERIT = "inherit",
    DEFAULT = "default"
}

export enum MuiSizeEnum {
    //"medium" | "small" | "large"
    MEDIUM = "medium",
    SMALL = "small",
    LARGE = "large"
}

export enum ColorEnum {
    //"primary" | "secondary" | "info" | "success" | "warning" | "inherit"
    PRIMARY = "primary",
    SECONDARY = "secondary",
    INFO = "info",
    SUCCES = "success",
    WARNING = "warning",
    INHERIT = "inherit",
}

export enum ChipColorEnum {
    // 'default' | 'primary' | 'secondary' | 'error' | 'info' | 'success' | 'warning'
    // 'primary' | 'secondary' | 'error' | 'info' | 'success' | 'warning' | 'default'
    PRIMARY = "primary",
    SECONDARY = "secondary",
    ERROR = "error",
    INFO = "info",
    SUCCES = "success",
    WARNING = "warning",
    DEFAULT = "default",
}