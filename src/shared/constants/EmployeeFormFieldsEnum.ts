export enum EmployeeFormFieldsEnum {
    USER_NAME = "userName",
    EMAIL = "email",
    PASSWORD = "password",
    STATUS = "status",
    ROLES = "roles"
}

export enum EmployeeFormContactEnum {
    NAME = "contact.name",
    LASTNAME = "contact.lastName",
    PHONENUMBER = "contact.phoneNumber",
    ADRESS = "contact.adress"
}